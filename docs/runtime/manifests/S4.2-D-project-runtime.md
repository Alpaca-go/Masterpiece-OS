# S4.2-D — Project Runtime and Operations

Domain: Project.

Handlers: `projects:list`, `create`, `get`, `remove`, `scan-assets`, `remove-asset`, `remove-batch`, `clear-assets`, `import-files`.

Old Owner: Desktop `project-store.ts` and business closures in Electron `index.ts`.

New Runtime Service: `@masterpiece/runtime-core/application/project-store.ts` plus `createProjectOperations`.

Moved: Project store, intake, identity, asset role helper and shared analysis/path contract implementation.

Re-exported: Five old Desktop module paths are `COMPATIBILITY_ONLY`.

Adapters: Native file/folder chooser handlers remain Desktop-only.

Electron imports before: 0 in moved implementation.

Electron imports after: 0 in Project Runtime and operations.

Golden coverage: Project intake/storage is upstream of every Golden family.

Tests: Runtime 6/6 PASS; Unit 725/725 PASS; CLI 40/40 PASS; Desktop build PASS; Web Smoke PASS (`ELECTRON` host); Golden 5/5 PASS; provider calls 0; Golden auto-update NO.

Rollback: Restore old modules and inline project handler closures; remove package operations.

Result: PASS.
