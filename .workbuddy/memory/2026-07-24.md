# 2026-07-24

## 新建并交付 feature/reference-asset-selection-protocol
- 工作树原有未提交的「参考资产选择协议」功能：`apps/desktop/src/main/asset-selection-protocol/`（index.ts + prompts.ts 新模块）、`reference-translation-service.ts` / `pipeline-service.ts` / `reference-style-reconstruction.ts` / `ReferenceTranslationWorkspace.tsx` / `shared/types.ts` 集成改动，以及 2 个测试文件。
- 新建分支 `feature/reference-asset-selection-protocol`，基于 origin 的 `cbe19aa`（注意：本地 `feature/reference-led-visual-direction` ref 停在更早的 `9927eaa`，是 cbe19aa 的祖先；工作树实际基于 cbe19aa）。
- 提交 `fe53fca`（9 文件，1290 增 / 31 删），父提交 cbe19aa，工作树已干净。
- 重新打包桌面端：`NODE_OPTIONS="--use-system-ca" npm --prefix apps/desktop run package:portable` → `BUILD_EXIT=0`，产物 `apps/desktop/release/Masterpiece-OS-Desktop-Portable-0.1.0-x64.exe`（112MB，已签名）。打包自动跑通 `verify:document-flows` 离线门禁。
- 打包踩坑：沙箱禁止**覆盖** release/ 内已存在文件（首次报 EPERM 写 builder-debug.yml）。清空 `release/` 后全量重建即干净退出 0（新增文件写入不受限，仅覆盖受限）。
- 待办：AGENTS.md 要求的「真实 provider 端到端 smoke 测试」本次未执行（无凭据/未授权），正式发布前仍需补齐。
