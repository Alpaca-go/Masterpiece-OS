# Archived provider smoke phases

These scripts preserve historical real-provider calibration evidence. They are
not part of the Desktop runtime, package scripts, release gate, or current
Short-Chain workflow. Paths and stage labels are retained only for audit
traceability; use the semantic smoke commands in `apps/desktop/package.json`
for current verification.
