# G02 Pre-Live Readiness

Source identity, boundary and independence pass. Deterministic role eligibility fails (`unknown` / `UNKNOWN_SOURCE`), so structured coverage, ground-truth anchors and timeout freeze remain blocked.

The generic verifier is repaired, but G02 is not ready for pre-live readiness. A future repair must change generic document-role handling or provide a new explicitly approved source that the existing classifier deterministically recognizes; this phase makes no production-semantic change.
