# S4.1R-B — Node Config and Runtime Paths

Purpose: Provide Node-compatible user-data, settings, project-data and prompt-root paths while preserving the current settings schema.

Old Host: Electron `app.getPath(...)` and Desktop settings adapter.

New Host: Node runtime paths and Node settings adapter.

Files Added: `apps/web-runtime/package.json`, `tsconfig.json`, `runtime-paths.ts`, `node-settings-store.ts` and adapter tests.

Files Modified: Root workspace scripts and lockfile.

Electron Dependencies Before: Config/path resolution required Electron.

Electron Dependencies After: Node config/path adapter imports Electron 0 times.

Desktop Dependencies Before: Config/path resolution imported Desktop settings store.

Desktop Dependencies After: Node config/path adapter imports Desktop 0 times.

Runtime Behavior: Settings schema, profile selection and `defaultDataPath` semantics preserved.

RPC Behavior: Unchanged.

WEB_SMOKE_HOST: ELECTRON.

Tests: Node adapter 3/3 PASS; Unit 733/733 PASS; CLI 40/40 PASS; Runtime 14/14 PASS; Web Smoke PASS.

Golden: 5/5 PASS; provider calls 0; auto-update NO.

Rollback: Remove the Node app adapter files and workspace entries.

Result: PASS.
