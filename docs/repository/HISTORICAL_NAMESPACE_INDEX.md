# Historical Namespace Index

| Historical name | Meaning | Current relationship |
|---|---|---|
| CLI v5 / prompts v5 | fifth analysis implementation and prompt generation | current implementation is now `analysis-engine` / `prompts/analysis`; persisted config filename remains compatible |
| vNext | former name for the Short-Chain generation generation | semantic implementation is `generation` + `short-chain-service`; persisted/API compatibility values remain |
| Phase9B | origin of the production Space compiler quality implementation | current compiler is `space/compiler.js`; trace/config evidence remains |
| R8.6 / R9 | Space baseline and productionization chronology | `r8_6_golden` and `phase9b_quality` remain accepted compatibility values selecting one compiler |
| R10 | reference and quality stabilization chronology | policies live under semantic Space modules |
| R11 / R11.1 / R11.2 | continuation and target-scene chronology | current ownership is `space/continuation` and `space/scene-projection` |
| V6 / V18 | creative schema/implementation history | retained only where schema migration, compatibility, fixtures, or history requires it |
| S1-S6 | repository stabilization phases | historical governance documents; never production namespaces |

Historical reports, Golden evidence, evaluation cases, and archaeology must keep these names when they are part of the evidence.

