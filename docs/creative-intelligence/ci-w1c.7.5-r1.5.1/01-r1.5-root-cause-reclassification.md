# R1.5 root-cause reclassification

R1.5 的历史结论 `HOLD_FOR_STRATEGIC_SYNTHESIS_REPAIR` 保持原样，不回写、不改分数。本阶段对其技术原因作独立重解释：Planning 已 PASS；两次 Strategic 请求均在收到 response headers 前以 `REQUEST_FAILED / UND_ERR_HEADERS_TIMEOUT` 结束，parser、structural validator 与 grounding gates 均未进入。因此没有证据表明 Strategic 内容失败。

R1.5.1 的技术归因是 `HOLD_FOR_PROVIDER_TRANSPORT_AND_TIMEOUT_CONTRACT_REPAIR`。未来 qualification 的判定边界为：没有 usable response 到达 parser/gates 时返回 `HOLD_FOR_PROVIDER_TRANSPORT_REPAIR`；response 已到达但 parse 或 gate 失败时返回 `HOLD_FOR_STRATEGIC_SYNTHESIS_REPAIR`。

本审计只读取仓库内既有 R1.5 redacted/runtime audit。没有读取真实 DOCX、外部父目录或 legacy PNG，也没有发起 provider、live model、health-check 或 connectivity-probe 调用。
