# G01 Attempt 5 readiness

Pre-live 条件现已满足：

- canonical request timeout 在 production bridge 到 transport 可执行；
- Planning/Strategic 独立 timeout policy 已冻结；
- transport retry 与 semantic repair 在 prompt、触发条件和计数上分离；
- Attempt 4 两次 timeout 可被稳定归类为 transport，不再冒充 semantic repair；
- future verdict 能区分 provider transport failure 与 Strategic content failure；
- redacted v2.1 evidence 可独立重算；
- Strategic-only `stopAfter: 'synthesis'`、trace、SG13 与 qualification review 契约保持不变。

Readiness：`READY_FOR_G01_ATTEMPT_5`。这是允许未来单次 qualification 的前置状态，不构成执行授权；本阶段没有运行 Attempt 5。
