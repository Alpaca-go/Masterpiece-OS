# S4.1R-F — Web Primary Start Migration

Purpose: make `web:dev` launch Node Host plus an independent Web Renderer.
Old Host: electron-vite / Electron Main.
New Host: Node + Vite.

Files added/modified: Node start orchestrator, `apps/web` renderer workspace, root scripts, Desktop renderer consumption config.
Electron dependencies before/after: 1 / 0.
Desktop dependencies before/after: 1 / 0.
Runtime/RPC behavior: unchanged.
Tests: actual `web:dev` health, renderer HTTP, project and generation routes PASS; Desktop build PASS.
Golden: 5/5, no update.
WEB_SMOKE_HOST: `NODE`.
Rollback: restore renderer ownership and previous root start command.
Result: `PASS`.
