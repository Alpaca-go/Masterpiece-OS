# S4.1R-C — Node Credential Adapter

Purpose: Supply credentials to the existing Shared Runtime boundary without Electron or Renderer secrets.

Old Host: Electron `safeStorage`.

New Host: Environment override plus Node-local AES-256-GCM credential adapter.

Files Added: `node-credential-store.ts` and `S4.1R-credential-compatibility.md`.

Files Modified: Node settings adapter only.

Electron Dependencies Before: Web credentials required Electron `safeStorage`.

Electron Dependencies After: Node credential adapter imports Electron 0 times.

Desktop Dependencies Before: Web credentials required Desktop settings store.

Desktop Dependencies After: Node credential adapter imports Desktop 0 times.

Runtime Behavior: Same `readCredentials(profileId)` contract and provider/model selection.

RPC Behavior: Settings responses remain secret-free.

WEB_SMOKE_HOST: ELECTRON.

Tests: Encrypted-at-rest, environment precedence and settings/profile compatibility PASS; full batch regression PASS.

Golden: 5/5 PASS; provider calls 0; auto-update NO.

Rollback: Remove Node credentials; Electron credentials remain untouched.

Result: PASS. No Electron credential was silently migrated.
