# S6 Version Topology Map

This map records ownership and compatibility, not an inferred numeric chronology.

## Visual analysis

```text
Web / Node Host
  -> PipelineService
     -> dynamic import apps/cli/src/v5/bootstrap.js       CURRENT authority
        -> apps/cli/prompts/v5                            CURRENT prompt set
        -> masterpiece-os-v5.json                         COMPATIBILITY artifact

Older analysis/report generations                        HISTORICAL / ARCHIVE
```

## Short-chain image generation

```text
Web creative workspace
  -> shared operation registry
     -> runtime-core vnext-service                        CURRENT orchestration service
        -> image-generation-runtime/src/vnext             CURRENT compiler/orchestrator namespace
           +-> Space family
           |   -> src/space/phase9b-space-compiler        CURRENT Space compiler
           |      <- r8_6_golden                          COMPATIBILITY config/default ID
           |      <- phase9b_quality                      COMPATIBILITY alias (same compiler)
           |      <- R9/R10/R11/R11.1/R11.2 citations    HISTORICAL rule provenance
           |      <- Golden/quality baselines             FIXTURE
           |
           +-> Packaging / VI / Poster prompt compiler    CURRENT non-Space compiler
           +-> vnext_legacy Space route                   COMPATIBILITY/debug fallback
```

`r8_6_golden` and `phase9b_quality` are two accepted names for one implementation. They are not parallel compiler authorities.

## Project visual context

```text
Pipeline completion
  -> project-context-vnext-builder                        CURRENT builder
  -> project-context service                              CURRENT reader/writer
  -> project-visual-context.vnext.json                    COMPATIBILITY persisted filename
  -> ProjectVisualContextVNext / getVNext                 COMPATIBILITY public/schema API
```

## Reference First and continuation

```text
Reference First reconstruction / beta closure             CURRENT reference authority
  -> VisualDecisionPacket / PromptSourceObject             CURRENT structured source
  -> short-chain generation orchestrator                   CURRENT execution authority
  -> Space reference policy / reference boundary           CURRENT Space policy

Continuation contract
  -> target-scene projection                               CURRENT target-scene authority
  -> continuation program override                         CURRENT continuation authority
  -> same Space compiler                                   CURRENT compiler (not a new version)
```

## Generic image utility path

```text
Reference/document preview and shared production helpers
  -> ImageGenerationWorkspace / ImageGenerationService     CURRENT utility generation authority

Formal Short-Chain creative flow
  -> VNextGenerationWorkspace / vnext service              CURRENT formal generation authority
```

These paths have different product responsibilities. They are not two authorities for the same capability.

## Compatibility and history boundary

```text
CURRENT semantic implementation
  <- COMPATIBILITY parser / operation alias / persisted identifier

CURRENT production  -X->  HISTORICAL runtime
CURRENT production  -X->  ARCHIVE
CURRENT production  -X->  evaluation fixtures
```

