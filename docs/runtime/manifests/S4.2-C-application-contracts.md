# S4.2-C — Application Runtime Contracts

Domain: Cross-domain application contracts.

Handlers: None.

Old Owner: `apps/desktop/src/shared/types.ts`.

New Runtime Service: `@masterpiece/runtime-core/application-contracts.ts`.

Moved: The complete application contract module, without semantic edits.

Re-exported: The old Desktop path is a `COMPATIBILITY_ONLY` package re-export.

Adapters: None.

Electron imports before: 0.

Electron imports after: 0.

Golden coverage: All runtime families consume these types; full regression required.

Tests: Unit 723/723 PASS; CLI 40/40 PASS; Desktop build PASS; Web Smoke PASS (`ELECTRON` host); Golden 5/5 PASS; provider calls 0; Golden auto-update NO.

Rollback: Move the contract module back and remove the package dependency/re-export.

Result: PASS.
