# S4.1R-G — Node Web Smoke Migration

Purpose: make `web:smoke` prove the actual primary Web path.
Old Host: Electron.
New Host: Node.

Coverage: readiness, health, real React renderer state and screenshot, settings/config, provider resolution without a call, analysis cancellation, Reference First, compile/start route errors, and process-tree guards.
Electron dependencies before/after: 1 / 0.
Desktop dependencies before/after: 1 / 0.
Tests: PASS with 18 descendants inspected while Chrome was active.
Golden: 5/5, no update.
WEB_SMOKE_HOST: `NODE`.
Rollback: revert smoke runner and root command.
Result: `PASS`.
