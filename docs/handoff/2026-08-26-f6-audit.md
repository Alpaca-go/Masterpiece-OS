# F6 Pre-Delete Audit — 2026-08-26 Short-Chain Convergence

> **Audience**: Next session that will execute F6.B (the actual delete).
> **Source**: F6.A read-only audit on `feat/theme-system-and-token-aliases`
> branch (HEAD `61b33cf8`, after F5 commit landed and pushed).
> **Goal**: Map every site that touches the legacy short-chain workspace tree
> so F6.B can delete safely without rediscovering consumers.

## 1. Scope of the deletion (F6.B)

Three production files + three test files + one test rewrite + one import
cleanup + one CSS comment block. All part of one logical change: "the legacy
`ShortChainGenerationWorkspace` tree is retired; the canonical
`ShortChainPage` (backed by `useShortChainBrief` + `useShortChainGeneration`)
is the sole short-chain UI entry."

### 1.1 Production files to delete

| Path | Lines | Notes |
|---|---|---|
| `apps/web/src/components/ShortChainGenerationWorkspace.tsx` | 1444 | The legacy component. App.tsx line 15 imports it but no JSX site renders it (the legacy path `screen === 'creative-session'` was already rerouted to `<ShortChainPage>` in commit `5dc13e4f` per P1.7a — see App.tsx lines 530-534 and the explanatory comment at lines 540-541). |
| `apps/web/src/reference-first/state.js` | 161 | Pure-logic state module used **only** by the legacy workspace. Exports: `MAX_SPACE_REFERENCE_IMAGES`, `SUPPORTED_REFERENCE_EXTENSIONS`, `isSupportedReferenceKind`, `isSupportedReferenceFile`, `validateReferenceHard`, `validateReferenceSoft`, `canUseGenerationBasis`, `toggleReferenceId`, `removeReferenceId`, `replaceReferenceIds`, `mergeUploadedReferenceIds`. NOT imported by any canonical hook (`useShortChainBrief` has its own inline `canCompile` memo at lines 125-131 and does not delegate to `state.js`). |
| `apps/web/src/continuation/ui-state.js` | 138 | Pure-logic state module used **only** by the legacy workspace. Exports continuation scene rules (`CONTINUATION_SCENE_CARDS`, `CONTINUATION_PRESERVE_COPY`, `CONTINUATION_REDESIGN_COPY`, `createContinuationTaskId`, `normalizeSceneId`, `isTargetSceneDisabled`, `isCustomSceneValid`, `canSubmitContinuation`, `continuationLineageLabel`, `CROSS_SCENE_ADVISORY_CODE`, `GENERATION_MODE_LABELS`, `generationModeLabel`, `referenceFirstCrossSceneAdvisory`, `findCrossSceneReference`). NOT yet imported by `useShortChainGeneration` (comment at lines 14-15: "P1.4 接入 PreviewCanvas; continuation 链路 P1.4 接入" — but the actual import line 17-25 doesn't reference `continuation/ui-state.js`). |

### 1.2 Production files to modify

| Path | Edit |
|---|---|
| `apps/web/src/App.tsx` | Remove `import { ShortChainGenerationWorkspace } from './components/ShortChainGenerationWorkspace';` (line 15). Remove the comment block at lines 540-541 (`// P1.7 路由切换: 用新 ShortChainPage (路线 A / §6 P1) 替代 v1 ShortChainGenerationWorkspace.\n// 旧的 Workspace 还保留 import 引用 (P3 退役阶段才删).`) — that comment was the only remaining justification for the legacy import. |
| `apps/web/src/styles.css` | Delete lines 2603-? (the `/* === Step 2 UI cleanup: shared field / stack / helper classes for ShortChainGenerationWorkspace === */` block). Need a follow-up scan to find the matching `=== end Step 2 ===` marker so the entire class block goes. Without the scan, dead CSS classes remain. |

### 1.3 Test files to delete (3)

| Path | Reason |
|---|---|
| `tests/image-generation/space-r10-reference-first-state.test.js` | Imports pure-logic module that gets deleted. Subject (`validateReferenceHard`, `canUseGenerationBasis`, etc.) has no remaining production consumer. |
| `tests/image-generation/space-r10-workflow-acceptance.test.js` | Same — only imports `canUseGenerationBasis` / `removeReferenceId` / `MAX_SPACE_REFERENCE_IMAGES` from the deleted `state.js`. |
| `tests/image-generation/space-r11-continuation-ui-state.test.js` | Same — imports from the deleted `continuation/ui-state.js`. |

### 1.4 Test files to rewrite (4)

These tests are not deletable — they cover invariant contracts that the
**canonical** ShortChain tree still owns. The edit is to replace the legacy
pointer with the canonical one.

| Path | Edit |
|---|---|
| `tests/runtime-application/image-generation-entry-points.test.ts:22` | Change `assert.match(appSource, /<ShortChainGenerationWorkspace/u);` to `assert.match(appSource, /<ShortChainPage/u);`. Line 21 (`assert.match(appSource, /setScreen\('creative-session'\)/);`) already matches App.tsx line 533 — no change. |
| `tests/runtime-application/short-chain-default-entry.test.ts:14` | Same change. Line 15 (`assert.doesNotMatch(appSource, /<CreativeSessionWorkspace/u);`) is already satisfied. |
| `tests/runtime-application/creative-task-reference-path-binding.test.ts:233` (test BD-17) | The assertion is `git diff --name-only <baseline> HEAD -- <workspace> <rpc-server>` must equal `''`. After F6.B the workspace is gone; the diff will list the deletion. **Two viable strategies** (pick in F6.B, document the choice): (a) **Move baseline forward**: rebase the baseline SHA to the F6.B commit itself (assertion becomes trivial — empty diff at HEAD). (b) **Drop the assertion**: this test was a corrective guard during the P3-D phase; P3-D is closed (see `docs/packaging/history/p3-d/`). The file diff assertion was protecting work that is already shipped. Recommend (b). |
| `tests/runtime-application/space-reference-path-binding.test.ts:237` | Same pattern as BD-17. Same recommendation (drop). |

### 1.5 Test file to rewrite heavily (1)

| Path | Edit |
|---|---|
| `tests/runtime-application/web-reference-upload-workflow.test.ts` | **Entire file** depends on `WORKSPACE = 'apps/web/src/components/ShortChainGenerationWorkspace.tsx'` (line 25, used in 20 `read(WORKSPACE)` sites). Without the file, the assertions either read empty strings or error. Two options: (a) Delete the whole test file. (b) Rewrite every `read(WORKSPACE)` to point at the canonical file (`apps/web/src/pages/ShortChainPage.tsx`) and re-derive the regex assertions against the new source. Recommend (a) — this test guards an old upload flow that is now owned by `BriefEditor` (in `apps/web/src/features/short-chain/BriefEditor.tsx`); the canonical upload flow already has its own integration coverage in `apps/web/src/features/short-chain/`. Document the deletion in F6.B commit message. |

### 1.6 Doc-only mentions (no action)

14 docs files reference `ShortChainGenerationWorkspace` (history, plans,
audits). These are historical record. F6.B does NOT edit them — they remain
accurate as history. The handoff doc (`docs/handoff/2026-08-25-audit.md`) is
read by future agents; the F6.B commit message will note "F6 complete" so
the next reader knows.

## 2. 6-region grep results (audit table)

### 2.1 `ShortChainGenerationWorkspace`

| Region | Hits | Notes |
|---|---|---|
| `apps/*` | 8 | `App.tsx` (import + comment), `ShortChainGenerationWorkspace.tsx` (self), `components/shortchain/ShortChainHeader.tsx`, `components/shortchain/ShortChainPreviewPanel.tsx`, `features/short-chain/brief-editor.css` (CSS selector prefix?), `features/short-chain/hooks/useShortChainBrief.ts` (comment), `features/short-chain/hooks/useShortChainGeneration.ts` (comment), `styles.css` (comment at line 2603). The latter 6 are comments/CSS class strings only; deleting the legacy file does NOT remove them automatically — they remain as `// ref: ShortChainGenerationWorkspace` historical breadcrumbs. Acceptable. |
| `apps/cli/` | 0 | OK |
| `apps/web-runtime/` | 0 | OK |
| `packages/*` (incl. `runtime-core/src/application/`) | **0** | OK — confirms no runtime/IPC consumer of the workspace component. |
| `tests/*` | 7 | 3 pure-comment, 2 regex assertions, 1 path-in-diff assertion, 1 source-read (20 sites). See §1.3-1.5. |
| `docs/*` | 14 | All historical. No edit. |

### 2.2 `reference-first/state.js`

| Region | Hits | Notes |
|---|---|---|
| `apps/*` | 1 | `ShortChainGenerationWorkspace.tsx` only. |
| `apps/cli/` | 0 | OK |
| `apps/web-runtime/` | 0 | OK |
| `packages/*` | 0 | OK |
| `tests/*` | 2 | `space-r10-reference-first-state.test.js`, `space-r10-workflow-acceptance.test.js`. Both pure-logic tests on the deleted module — delete with the module. |
| `docs/*` | (not separately searched — included in workspace 14 hits above) | |

### 2.3 `continuation/ui-state.js`

| Region | Hits | Notes |
|---|---|---|
| `apps/*` | 1 | `ShortChainGenerationWorkspace.tsx` only. |
| `apps/cli/` | 0 | OK |
| `apps/web-runtime/` | 0 | OK |
| `packages/*` | 0 | OK |
| `tests/*` | 1 | `space-r11-continuation-ui-state.test.js`. Delete with the module. |
| `docs/*` | (included in workspace 14) | |

## 3. Canonical entry confirmation

`apps/web/src/pages/ShortChainPage.tsx` (lines 37-43):

```tsx
const brief = useShortChainBrief();
const gen = useShortChainGeneration();
```

Both hooks exist and are imported. `BriefEditor` + `PreviewCanvas` + `RightPanel`
all render real content backed by the hooks. The canonical path is **fully
wired**, not a placeholder. The legacy workspace is dead-weight — confirmed.

## 4. Scope decision summary for F6.B

| Action | Count | Files |
|---|---|---|
| Delete production file | 3 | workspace.tsx, state.js, ui-state.js |
| Modify production file | 2 | App.tsx (import + comment), styles.css (comment block) |
| Delete test file | 3 | space-r10 (×2), space-r11 |
| Rewrite test file (1-line) | 2 | image-generation-entry-points, short-chain-default-entry |
| Rewrite test (drop assertion) | 2 | creative-task-reference-path-binding (BD-17), space-reference-path-binding (BD-18 mirror) |
| Delete test file (heavy) | 1 | web-reference-upload-workflow |
| Doc-only (no action) | 14 | historical |

**Net**: 6 production files touched (3 delete + 2 modify + 1 css block delete),
8 test files touched (4 delete + 2 minor edit + 2 assertion drop).

## 5. Risk register

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| F6.B commit breaks test count below 1714 | High | High | Pre-define expected new test count: 1714 - 6 (3 deleted pure-logic tests, which are single-file single-test-count; 1 deleted heavy test; 2 assertion drops don't change count, but if dropping BD-17/18 removes test cases from their `describe` blocks, count changes). Estimate: 1714 → 1705-1710. Re-run after F6.B. |
| CSS comment removal breaks selector cascade | Low | Medium | The `Step 2 UI cleanup` block in styles.css is a documentation comment followed by helper class definitions. Helper classes are **also used by `BriefEditor` / `PreviewCanvas`** (which use the same `.short-chain-*` BEM prefixes). **Re-scan required** in F6.B to confirm the comment-block extent before deleting. |
| `<ShortChainPage` regex doesn't match because of import path or self-closing tag form | Low | Low | `ShortChainPage.tsx` line 37: `export function ShortChainPage(...)`. App.tsx line 530: `<ShortChainPage`. Match will succeed. |
| Dropping BD-17 / BD-18 removes regression guard for `apps/web-runtime/src/local-rpc-server.ts` (still asserted at line 233 alongside the workspace) | Low | Medium | Both tests assert the workspace path. F6.B removes the workspace path → those assertions need to either drop the workspace arg or drop the whole test. If we drop the whole test (recommended), the local-rpc-server assertion is lost. **Mitigation**: add a separate small test (`local-rpc-server-d3-stable.test.ts`) asserting `git diff --name-only <baseline> HEAD -- apps/web-runtime/src/local-rpc-server.ts` is empty, OR accept the gap and document it. |

## 6. Verification gate (for F6.B)

1. `npm run web:typecheck` 0 error
2. `npm run web:build` succeeds
3. `npm test` count change explained (target: 1705-1710)
4. `git grep ShortChainGenerationWorkspace` returns 0 in `apps/` and `tests/` (docs excluded — historical)
5. `git grep reference-first/state` returns 0 in `apps/` and `tests/`
6. `git grep continuation/ui-state` returns 0 in `apps/` and `tests/`
7. styles.css still byte-counts within ±0.5 KB (acceptable noise; not the F7 refactor target)

## 7. If you need to roll back F6.B

Forward-only (per handoff §8). `git revert <F6.B SHA>` restores all files
including the 3 deleted test files and the 3 deleted source files.
