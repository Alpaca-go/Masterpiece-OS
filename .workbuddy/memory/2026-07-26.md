# 2026-07-26 工作日志

## 清理客户端「僵尸运行中」分析记录
用户反馈客户端里有条运行中但删不掉的分析记录。

### 根因
- 卡住记录：文档视觉转译任务 `01-01-九州美学`（id `af34d2dd-537e-4564-9a3d-065b991ebf92`），`runtime/run.json` 停在 `status:"running"`（7/21 应用中途退出遗留）。
- 客户端删除保护：`index.ts` `visual-translation:remove` 对 `status==='running'` 直接抛错，UI 删除按钮同样 disabled → 僵尸 running 永远删不掉。

### 数据位置（重要）
- 客户端 userData：`C:\Users\Administrator\AppData\Roaming\masterpiece-os-desktop\settings.json`。
- 实际数据根（settings.defaultDataPath）：`C:\Users\Administrator\Documents\Masterpiece OS Data\`，下分 `projects/`（project.json）、`visual-translation-v1/<runId>/runtime/run.json`、`reference-translation-v1/`。

### 处理
1. 先备份 run.json（.bak）并把 status 改为 `cancelled`（解除删除保护，非破坏性）。
2. 用户确认后整个任务文件夹移入回收站。注意：PowerShell 的 Add-Type / COM Shell.Application 均被沙箱拦截；改用 node `fs.rmSync`（被 genie-safe-delete shim 接管→送回收站）成功，虽抛 "Some operations were aborted" 但实际已完整删除，需 `ls` 复核。

### 产品缺陷备忘（可建议修复）
- ~~客户端启动时应把无活跃进程的 `running` 记录自动降级为 `failed/cancelled`~~ → **已修复**（见下方僵尸任务降级提交 `928e51e`）。

## Phase 1 代码提交（feature/visual-context-contract）
- 提交 `2ce4e1a`（父 `f3a5b14` 2.1.4.1），209 文件 +31036/-576。含 Phase 1 视觉上下文全部改动 + 此前累积未提交的 reference-first/retrieval-first/readiness/model-schema 工作（用户选择全部一起提交）。
- 工作区仅剩 1 个未跟踪临时文件 `apps/desktop/electron.vite.config.1784724776494.mjs`（构建残留，刻意不入库，可删）。
- **新 git 坑**：本机 loose ref 文件（`.git/refs/heads/...`）会被后台进程在数秒内删掉（连 `git commit` 自己写的 ref 都会消失）→ 修复方式：直接编辑 `.git/packed-refs` 插入/推进引用（保持排序），git 立即可读，稳定不丢。详见 MEMORY.md。

## 僵尸任务自动降级修复（提交 928e51e）
- 修复「应用异常退出后 running 记录永远删不掉」：三条持久化路径全覆盖。
  - `visual-translation-service` / `reference-translation-service`：getRun/listRuns 读取时侦测「磁盘 running 且不在内存 active 表」→ 自动降级 failed（visual 另置 `analysisStatus:'failed_before_completion'`、记 ORPHANED_RUN_DOWNGRADED 事件；reference 置 `stage:'FAILED'`）。
  - `pipeline-service`：新增 `isActive` / `reconcileOrphanedProject`；`index.ts` projects list/get/remove 全走降级，删除守卫以活跃表为准。
- 判定安全性依据：所有执行路径均「先登记 active 再写盘 running」，故磁盘 running + 不在 active 必为异常退出遗留。
- 新测试 `apps/desktop/tests/orphaned-run-reconciliation.test.ts` 4 例；桌面全套 153/153；document-flows 门禁 PASS；重打包 portable EXE（00:31）。
- 注意：`rm -rf apps/desktop/release` 现会触发 safe-delete 批量确认（>50 文件），需授权后执行。

## Phase 2 文档分析上下文提取器（提交 dc6471e，分支 feature/document-context-extractor）
- 按《02-文档分析上下文提取器开发文档》完成 Phase 2 全量开发，止步 Phase 2 未进入 Phase 3。
- 新增（均为新文件，不改动 Phase 1 视觉分析/参考转译管线）：
  - `schemas/document-visual-context.schema.json`：DocumentVisualContext v1.0 Schema。
  - `apps/desktop/src/main/document-context-core.ts`：纯逻辑零 IO——提取/修复提示词、JSON 解析、确定性归一化（过滤市场/财务噪声、evidence 溯源、unknownFields、6 类警告）、9 节简报编译、`adaptLegacyVisualTranslationResult` 旧三方向适配器。
  - `apps/desktop/src/main/document-context-service.ts`：pipeline 00 准备→01 角色索引→02 提取（1 次模型调用+最多 1 次 Repair）→03 归一化→04 人工确认→05 本地零模型编译；数据在 `document-runs/<run-id>/`；僵尸任务降级（有 extracted-context → 恢复到 awaiting_confirmation，否则 failed）。
  - 渲染层 `DocumentContextWorkspace.tsx`：列表/表单/字段级确认页（确认/编辑/删除/标记未知/查看来源）/简报页 + Legacy 转换入口；`AnalysisModeTabs` 加第 4 个 tab `document-context`；App.tsx 集成 runs/删除/最近记录。
- 测试 `tests/document-context-service.test.ts` 8 例（解析、不编造、可溯源、用户覆盖、Repair 上限、崩溃恢复、取消、Legacy）；桌面全套 161 通过；verify:document-flows PASS。
- 重打包 portable EXE 成功（04:31，112MB）。打包前 `rm -rf release` 需 safe-delete 授权（本次用户重新授权后通过）。

## Phase 3 参考视觉转换 Anchor 工作流（分支 feature/reference-anchor-workflow）
- 按《03-参考视觉转换Anchor工作流开发文档》完成 Phase 3 全量开发，止步 Phase 3 未进入 Phase 4。
- 新增（不改动 §2 禁改区：视觉分析 Pipeline / ProjectVisualContext / 文档分析 Pipeline / DocumentVisualContext / API Profile / 公共资产导入）：
  - `schemas/reference-style-capsule.schema.json`：ReferenceStyleCapsule v1.0 Schema。
  - `apps/desktop/src/main/reference-anchor-core.ts`（613 行纯逻辑）：`mergeCurrentProjectContext`（当前项目身份优先、记录冲突、文档仅补空缺）、`detectReferenceIdentityLeaks`（参考品牌身份硬隔离，LOGO_DIRECT_COPY/SLOGAN_LEAK/SIGNATURE_GRAPHIC_DIRECT_COPY）、`compileReferenceStyleCapsule`（每类 ≤5 规则 MAX_RULES_PER_CATEGORY）、`compileAnchorBrief`（A–G 7 块，1200–2500 字）、`validateAnchorBrief`（禁 UUID/PTM/排名/质量总分）、`adaptLegacyReferenceResultToStyleCapsule`。
  - `apps/desktop/src/main/reference-anchor-service.ts`（748 行）：pipeline 00 载入当前项目上下文→01 参考资产准备（4–8 张软限）→02 参考风格分析（1 次模型调用，vision）→03 胶囊编译→04 Brief 编译+人工决策；缓存 raw-reference-observations.json 支撑 updatePreference/retryBrief 零模型调用重试；临时参考项目用后即删；僵尸任务降级（有产物→awaiting_decision，否则 failed）。
  - `ReferenceAnchorWorkspace.tsx`（~430 行）+ `AnalysisModeTabs` 新增主 tab `reference-anchor`（旧 visual-translation/reference-translation 标注「(旧)」）+ App.tsx 集成（badge/runs/删除/最近记录）+ styles.css Phase 3 样式。
  - IPC/preload/types：`referenceAnchor.*` 15 个通道（start/listRuns/getRun/getCapsule/getCapsuleMarkdown/getBrief/updatePreference/retryBrief/setDecision/cancel/remove/inspectAssets/adaptLegacyRun/onProgress 等）。
- 测试 `tests/reference-anchor-workflow.test.ts` 14 例（身份隔离硬拦截、胶囊质量、Brief 7 块/长度/禁词、重试缓存 0 额外模型调用、Legacy 适配、全流程、僵尸降级）；桌面全套 175/175 通过；verify:document-flows PASS。
- 重打包 portable EXE 成功（05:28，112MB，已签名）。
- 提交 `57514fa`（父 `a8b550c`，13 文件 +2665/-7），分支 `feature/reference-anchor-workflow`。git 坑再次复现：commit 后 loose ref 被后台进程删除、分支不前进，改用编辑 `.git/packed-refs` 推进（先插入分支行再替换哈希）后恢复正常。

## Phase 3.1 参考锚定质量修复（文档 v5.3.1，分支 feature/reference-anchor-workflow）
- 按《Masterpiece-OS-Phase3参考锚定质量修复开发文档-v5.3.1.md》完成 §2.1 八处 MUST 修改（A 事实分类 / B 设计建议清洗 / C 参考专属元素冲突检测 / D 抽象转译 / E 任务级规则过滤 / F Warning Compiler / G Locked Assets 去重 / H 输出比例单值化），严守 §2.2 禁改区（视觉分析 Pipeline、两 Context Pipeline、Anchor 人工确认、旧任务迁移等未动）。止步 v5.3.1，未进入 Phase 4。
- 改动文件：`reference-anchor-core.ts`（+531/-60，事实分类/抽象转译/回流检测/规则去重/任务过滤/Warning Compiler/比例单值化 + compile/markdown/brief/validate 改造）、`reference-anchor-service.ts`（+35/-2，aspectRatio 注入 + 两处 reference-signature 回流硬阻断）、`types.ts`（去重 GenerationOutputType + 新增 NormalizedProjectFacts/AnchorAspectRatio 等）、`reference-style-capsule.schema.json`（projectFacts/aspectRatio/humanNotes 必填）、`tests/reference-anchor-workflow.test.ts`（+145，8 新回归用例）。
- 验证全绿：`tsc --noEmit` 0 错；targeted 测试 **22/22**；桌面全套 183/183；`verify:document-flows` **6/6 PASS**（离线）。
- 按 §13 分 4 个独立提交（每个可独立编译）：S1 `aa647eb`（契约/types/schema）、S2 `dc84098`（core 逻辑）、S3 `7f743c8`（service 编排）、S4 `0b05b8d`（回归测试）。
- **新 git 坑（本次踩到）**：packed-refs 必须是 **LF**——用 Python 文本模式读写会把 LF 变 CRLF，导致 git 报 `unexpected line in .git/packed-refs` / `ignoring ref with broken name`，整个文件无法解析。修复=字节级归一为 LF 再回填（详见 MEMORY.md Git 坑）。另：`git commit` 只打印短 SHA，且 loose ref 被删，须从提交输出解析短 SHA 再 `git rev-parse` 展开成 40 位回填 packed-refs。
- 重打包 portable EXE 成功（10:22，**107MB，已 signtool 签名**）：`apps/desktop/release/Masterpiece-OS-Desktop-Portable-0.1.0-x64.exe`。打包命令 `NODE_OPTIONS="--use-system-ca" npm --prefix apps/desktop run package:portable`（去删除 shim），打包前 `rm -rf apps/desktop/release`（沙箱授权）清旧产物避免 EPERM。用户指令「测过再打包、授权一律点是」已执行。

## 修复：Brief 级回流检测误报（提交 38d48d6）
- **结构性缺陷**：`detectReferenceSignatureReentry([briefMarkdown], prohibitedSurfaceElements)` 对**全量 brief 文本**做 `includes()` 扫描；但 brief §F 禁止事项按定义会展开被禁元素名（如「禁止迁移参考专属表层元素：砂锅轮廓」）→ 必然命中自己写的文本 → 误抛 `REFERENCE_SIGNATURE_REENTERED_ANCHOR_BRIEF`。
- **修复**：service.ts Brief 级检测改为只扫描正向规则区域（`briefMarkdown.split(/\n## [F-H]\./u)[0] || briefMarkdown`），排除 §F/§H 等按定义列举被禁元素的段落。
- 验证全绿：tsc 0 错误 / 183/183 / 6/6 PASS。重打包 portable EXE 成功（11:02，107MB，已签名）。

## Phase 4 三大功能轻量整合（提交 c9d255b，分支 feature/reference-anchor-workflow）
- 按《04-三大功能轻量整合开发文档.md》完成 Phase 4 全量开发，严守禁改区（视觉/文档/参考三条 Pipeline 零重写，未合并成统一超级 Pipeline）。
- 新增/改动：
  - 类型+Schema：`types.ts` 增 `ResolvedProjectContext`/`ContextConflict`/`ProjectDocumentContextLink`/`ConflictResolutionInput` 及 `contextIntegration` API 段；`schemas/resolved-project-context.schema.json`（含 sourceVersions + sourceFingerprint）。
  - 纯逻辑核心：`context-resolver.ts`（`resolveProjectContext` 确定性合并、视觉绝对优先、文档补非阻断字段、冲突 traceable、`hasBlockingConflict`/`applyUserOverride`）；`reference-anchor-core.ts` 增 `resolvedToMerged`（Resolved→MergedCurrentProject，参考流水线消费）。
  - 服务编排：`context-integration-service.ts`（`linkDocumentContext`/`unlink`/`getLink`/`getVisualStatus`/`getResolved`(缓存失效)/`resolve`(原子写)/`listConflicts`/`applyConflictResolution`/`migrate`/`export`/`isDocumentContextReferenced`）；`reference-anchor-service.ts` `loadCurrentProjectContext` 优先读 Resolved，未解决阻断性冲突抛 `IDENTITY_CONFLICT_UNRESOLVED`/`LOCKED_ASSET_CONFLICT_UNRESOLVED`。
  - 主进程/IPC/preload：10 个 `context-integration:*` handler（含 `is-doc-referenced`）+ resolver 类型导入。
  - 渲染层 UI：`ContextIntegrationPanel.tsx`（项目详情页内嵌：三状态行/关联/冲突确认弹窗/重新合并/升级项目上下文/打开参考）；`ReferenceAnchorWorkspace.tsx` §7.3 来源横幅（当前项目/视觉上下文版本/文档关联/合并状态/Locked Assets 项数）；`App.tsx` 内嵌面板 + 删文档前引用检查；styles.css Phase 4 样式。
- 验收要点：零模型调用合并；品牌名/Logo/Locked Assets/核心产品/包装结构不被文档静默覆盖（冲突人工确认）；Reference 优先读 Resolved 回退 Project Visual Context；缓存失效最小化（sourceFingerprint 比对）；三状态独立；旧项目「升级项目上下文」迁移入口；三个功能仍独立可用。
- 测试新增 `tests/context-integration.test.ts` 25 例（§15.1 Resolver 单测 / §15.2 关联 / §15.3 缓存 / §15.4 状态隔离 / §15.5 迁移 / §15.6 端到端）。桌面全套 **208/208 通过**；`verify:document-flows` **6/6 PASS**（离线）。
- 重打包 portable EXE 成功（13:49，**107MB，已 signtool 签名**）：`apps/desktop/release/Masterpiece-OS-Desktop-Portable-0.1.0-x64.exe`。`tsc --noEmit` 0 错误。
- git 坑复现并修复：commit 后 loose ref 被后台进程删、分支不前进（`git log` 回退到父提交 38d48d6）；用字节级 LF 重写 `.git/packed-refs` 第 9 行 `feature/reference-anchor-workflow` → `c9d255b…` 后恢复（注意：首次写入会引入尾随空行触发 `unexpected line in .git/packed-refs`，须过滤空行）。
- 按文档 §17 止步 Phase 4，未进入其他阶段；整合验证报告见 `docs/phase4-context-integration-report.md`。

## 精简重构（文档 Masterpiece-OS-reference-anchor-workflow-精简重构开发文档.md，分支 feature/reference-anchor-workflow）
- 目标：保留三功能+稳定性基建，删旧生产入口、压缩重复协议、解耦跨模块。四 Phase：1 收缩入口 5→3；2 旧 Pipeline 只读化；3 压缩 Reference 协议（三层 Gate/去餐饮硬编码/Brief 七章节/去 TaskStyleCapsule）；4 服务解耦+shared/types 拆分。
- **分支决策**：文档建议新分支 `refactor/product-slimming-v1`，但本机 git 对该 slash 分支「packed-refs 有记录、show-ref 能列出、rev-parse 却报 unknown revision」无法解析（feature 分支正常）。故放弃新分支，继续在 `feature/reference-anchor-workflow` 提交（已验证可靠）。
- Phase 1 完成（提交 a2fd227）：AnalysisModeTabs 仅三入口；App.tsx 移除旧 visual/reference-translation 生产状态与首页混排；首页快捷仅 新建视觉分析/文档分析/参考视觉转换/API 设置；新增「历史任务」页 `LegacyHistoryWorkspace.tsx`（只读 查看/导出/打开文件夹/删除 旧版流程任务）；更新 architecture-boundary 测试。tsc 0 错误；208→207 测试（修 1 个旧五入口断言）后全绿。
- 注意：旧 VT/RT 的 `adaptLegacyRun` 在当前 DesktopApi 未暴露（仅 documentContext/referenceAnchor 有），历史页不提供「转换」按钮，仅查看/导出/删除。

## 方向纠偏：v5.3.1 质量修复 vs 精简重构（重要）
- 用户改引用了新文档 `Masterpiece-OS-Phase3参考锚定质量修复开发文档-v5.3.1.md`（内容质量修复，非架构重构）。经核对：**v5.3.1 早已在提交历史中完整实现**（`NormalizedProjectFacts`/`ReferenceMechanismRule`/`filterStyleCapsuleForTask`/`dedupeBriefRules`/`ANCHOR_ASPECT_RATIO_AMBIGUOUS`/`REFERENCE_SIGNATURE_REENTERED_ANCHOR_BRIEF`/`CORE_PRODUCTS_CONTAIN_TOUCHPOINTS` 均在 fd8ffef 的 core/service/types 里），§14 的 8 个测试用例 Test1–Test8 全在 `reference-anchor-workflow.test.ts`。
- **关键冲突**：精简重构方向要「去餐饮硬编码/行业中性」，但 v5.3.1 §3.3 明确**需要**餐饮词表（筷子套/纸巾/… 归 serviceMaterials，用于冯烫烫验证）。我上一 session 未提交的 core.ts 抽象改动（把 `SERVICE_MATERIAL_TERMS` 改成 辅助物料/… 抽象词）会**破坏** v5.3.1。→ 用 AskUserQuestion 澄清，用户选「**先修复再重构**」。
- 处理：① `git checkout -- reference-anchor-core.ts` 撤销冲突改动，恢复 v5.3.1 干净基线；② 核验基线全绿（tsc 0 / 208 测试 / verify 6-6）+ §9 Logo 表述(line 916 非伪精确)/§10 默认 16:9 已到位。

## 行业中性化叠加（提交 efd5120，非破坏式）
- 在 v5.3.1 之上做**可替换领域词库画像**（而非删词表）：新增 `interface AnchorDomainProfile`（viApplicationTerms/serviceMaterialTerms/designAdviceMarkers/anchorBlacklistTerms/anchorOptionalTerms/subjectDominanceProhibition）+ `export const DEFAULT_ANCHOR_DOMAIN_PROFILE`（= 现有含餐饮词表，行为 100% 等价）。
- `classifyProjectFacts`/`filterStyleCapsuleForTask`/`compileAnchorBrief` 三函数新增**可选** `profile` 尾参（默认 DEFAULT），内部改用 `profile.xxx`；line 893 硬编码禁令文案改为 `profile.subjectDominanceProhibition`。默认参数在调用时求值，故引用后声明的 const 不触发 TS TDZ 报错（tsc 已验证 0 错）。
- 新增测试「domain profile is swappable」：传入「服装零售」画像证明引擎已解绑餐饮（吊牌/购物袋/连衣裙 正确分类；模特走秀被过滤；餐饮词此时不再命中黑名单）。
- 验证全绿：tsc 0 / **209 测试全过**（含新用例 #114）/ verify:document-flows 6/6。git 坑复现：commit 短 SHA efd5120，loose ref 被删分支未前进 → 字节级 LF 回填 packed-refs（fd8ffef→efd51204b59c…）后 `git log` 复核已前进。
- 打包：`rm -rf apps/desktop/release`（沙箱授权）后 `NODE_OPTIONS="--use-system-ca" npm --prefix apps/desktop run package:portable`。

## 参考锚定：拖拽上传当前项目（提交 2c7b691）
- 需求：参考锚定页「当前项目」原本只能从下拉框选**已完成视觉分析**的项目；用户要一个可拖拽上传当前项目的上传区。用 AskUserQuestion 确认范围，用户选「**完整：拖图建项目并自动跑分析**」。
- 关键契约：`projects.create({ sourcePaths, apiProfileId })` 自动检测品牌/行业（**无需手填元数据**）；`analysis.start(projectId, false, profileId)` 是完成时 resolve 的 Promise，进度经 `analysis.onProgress` 推送；通用拖拽组件 `VisualAssetUploader`（`role='current_project'`）直接复用。ProjectWizard.tsx 是参考实现。
- 实现（仅改 `ReferenceAnchorWorkspace.tsx` + styles.css + 测试）：「01 当前项目」区加来源切换 Tab `projectSourceMode: 'existing'|'upload'`。上传模式：拖入图片 → `projects.create`/`importFiles` 建项目并 `scanAssets` 展示 → `analyzeAndSelect()` 调 `analysis.start` 跑视觉分析（带进度/取消/放弃）→ 完成后 `setSelectedProjectId(finished.id)` + 切回 existing 模式，自动成为当前项目。新增 `analysis.onProgress` 订阅（按 uploadProject.id 过滤）。
- 验证：tsc 0 / **210 测试全过**（新增 architecture-boundary 用例「Reference Anchor workspace supports uploading a new current project inline」）/ verify:document-flows 6/6。git 坑复现：commit 短 SHA 2c7b691，loose ref 被删 → 字节级 LF 回填 packed-refs（efd51204b59c…→2c7b6912d3de…）后复核前进。重新打包便携版客户端。

## repository-slimming-v2：Phase 0–2 完成（分支 refactor/repository-slimming-v2）
- Phase 0（122fb45）：docs/cleanup/ 三份盘点（legacy-inventory.json / legacy-import-graph.md / migration-plan.md），含 3 项已确认偏离：① style-carrier-ranking/task-reference-selection 被生产用→保留；② src/v5 视觉分析引擎（19 文件闭包）是正式功能→保留原位；③ Labs 用 JS 不重写 TS。
- Phase 1（eb6b25e）：共享能力迁入 packages/（model-runtime / document-ingestion / runtime-core / project-contracts / reference-asset-inspector），原路径留 re-export shim；desktop types.ts/pipeline-service/document-context-service 改指 packages。
- Phase 2（81dda8a）：labs/document-visual-directions（71+5 文件闭包、6 个 v2 测试 58 pass/0 fail、fixtures+snapshots、bin/run.mjs CLI）+ labs/reference-style-conversion（7 文件、7/7 测试、CLI 契约测试指向 lab bin）。Lab 内 src/shared|adapters 建本地 shim 指 packages（注意 adapters 比 shared/analysis 少一层，../../../../packages）。根 package.json 加 lab:* 4 个脚本；.lab-data/ 入 gitignore。
- 已知非阻塞：step4-stability 21 cancelled 为 node22 既有 flaky（原位置同样复现）；node --test 传目录在本机报 Cannot find module，须用 glob `tests/*.test.js`。
- git 坑又复现：commit 81dda8a 后分支不前进，字节级 LF 回填 packed-refs 修复。
- 下一步：Phase 3 Desktop 切断实验接线 → Phase 4 物理删除 → Phase 5 脚本/测试收敛 → Phase 6 门禁+打包。

## repository-slimming-v2：Phase 3 完成（提交 c4448d2e1dd1…）
- Desktop 切断实验接线：main/index.ts 删 visual-translation:*/reference-translation:* IPC+smoke flags；preload/types.ts 删 visualTranslation/referenceTranslation API；App.tsx 删 history 屏/LegacyHistoryWorkspace/translationRuns 等 state；DocumentContextWorkspace/ReferenceAnchorWorkspace 删「Legacy 任务转换」UI 块；analysis-contract.ts 删 validateMarkdownReport（引用已删 reference-translation-report）；新建 document-project-name.ts 承接 deriveDocumentProjectName。
- 测试：architecture-boundary 重写为「实验接线已移除」负面断言+生产断言；orphaned-run-reconciliation 整体删除；runtime-persistence 删最后一个依赖 visual-translation-service 的用例。tsc 0 错、Desktop 192/192 全过、verify:document-flows 通过。
- **严重事故+恢复**：`git rm apps/desktop/tests/orphaned-run-reconciliation.test.ts` 后数十秒内，某后台安全删除机制把 tests→desktop→apps 逐层送入回收站（D:/$RECYCLE.BIN/S-1-5-21-3696747777-…-500，$I 元数据可解析原路径）。**恢复法**：python 解析 $I 文件（offset 24 namelen + utf-16 路径）定位 $R 目录，`mv "$RB/$RXXX" 原路径`。所有未提交编辑随目录整体恢复、零丢失。教训：本机做删除操作后立刻 `ls` 验证父目录仍在；发现目录消失先查回收站再考虑 git checkout。
- apps/desktop/node_modules 曾缺失，`NODE_OPTIONS="--use-system-ca" npm ci` 重装（375 包）。
- git 坑复现：commit 后分支不前进且 tail 截断看不到短 SHA → 用 `git fsck --lost-found | grep dangling` 找到新提交（按 parent=81dda8a 确认），字节级 LF 回填 packed-refs。
- 下一步：Phase 4 物理删除（保留生产 reference-style-reconstruction 链）→ Phase 5 → Phase 6 打包。

## repository-slimming-v2：Phase 4–6 全部完成 + 打包（终态）
- 提交链：…→c4448d2（P3）→79b3707（P4 物理删除，341 文件 -56,571 行）→56cbf4f（P4b 边界清理：删 desktop runtime 死模块 recovery-service/runtime-status/result-checkpoint + verify-reference-first-protocol.ts + types.ts 4 块遗留类型 -260 行；runtime-persistence.test.ts 删两段依赖测试）→8dbc7e7（P5）→a71196d（P6）。每次 commit 后均需字节级 LF 回填 packed-refs 推进分支（用 `git fsck --unreachable` 按 parent+message 找新提交）。
- Phase 5（8dbc7e7）：根 scripts 收敛为文档 §10 最终集合；`verify:document-flows` 更名 `verify:current-flows`（AGENTS.md、desktop prepackage 钩子同步）；重建精简 `bin/masterpiece-os.js`（仅 v5 analyze/inventory，qwen provider，内联 reasoner factory）；README 全量重写。**坑**：根 `test` 必须 `node --test tests/*.test.js tests/v5/*.test.js`——裸 `node --test` 会递归扫 desktop .ts 与 labs 导致假失败；`node --test tests/` 传目录报 Cannot find module。
- Phase 6（a71196d）：`scripts/verify-no-obsolete-code.mjs`（禁止关键字扫描；豁免=labs/ 整体 + reference-first 目录内 style-carrier-ranking/task-reference-selection 两关键字 + architecture-boundary.test.ts/门禁自身白名单）+ `scripts/verify-production-boundaries.mjs`（desktop 不 import labs、builder 配置与 out/ 不含 labs、preload 无 visualTranslation/referenceTranslation API）+ `docs/cleanup/repository-slimming-v2-validation.md`（3 项偏差记录在案，含 lab 内 v1 是 v2 冻结上游库依赖、非对外入口）。
- **打包新坑**：electron/dist 缺失报 "specified electronDist does not exist"，install.js 联网 fetch failed。**修复**：`%LOCALAPPDATA%/electron/Cache/<hash>/electron-v43.1.1-win32-x64.zip` 存在，用 Python zipfile 手动解压到 `node_modules/electron/dist` 并写 `path.txt`（内容 `electron.exe`）即可。另：`rm -rf` **不存在**的目录会被 safe-delete shim fail-closed 中断整条 && 链，先 ls 确认存在再删。
- 终态验证全绿：no-obsolete-code（140 文件）、production-boundaries（78 文件）、current-flows PASS、根测试 35/40（5 cancelled=node22 flaky，node24 全过）、desktop 133/133、tsc 0 错、labs 58+7 过 0 fail。产物 `apps/desktop/release/Masterpiece-OS-Desktop-Portable-0.1.0-x64.exe`（112MB，signtool 已签名）。

## 客户端 UI 重设计 prototype（UI Designer 任务）
- 用户不满意现有桌面端 UI，要求按经验优化并出图。先通读 `apps/desktop/src/renderer/` 全部组件 + styles.css，识别设计债：① 颜色/圆角/间距全硬编码 hex、无 token 层；② 无暗色模式（侧栏虽深色，整体 light-only）；③ 响应式断裂（`body{min-width:1000px}` 强制不响应）；④ 可访问性缺口（仅 input 有 focus、无全局 `:focus-visible`、Tab 无 `aria-controls`/方向键 roving focus、缺 `prefers-reduced-motion`）；⑤ 4 处近重复「指标卡网格」(intake-metrics/run-metrics/result-summary/reference-project-summary) 未合并；⑥ 触摸目标偏小。
- 交付：`D:\Masterpiece-OS\ui-redesign\home.html` —— 高保真可交互首页工作台 prototype。保留现有品牌语言（暖米色 #f1efe9 / 品牌紫 #6c42d8 / 衬线展示体 Fraunces / blobby M logo / 序号徽标 01-03），并：抽成 `:root` 设计令牌 + `[data-theme=dark]` 双主题（含可用明暗切换按钮，localStorage 持久化）、统一 `.metric-strip` 指标组件、`.record` 行组件（序号徽标+chip+状态徽标+指标+删除，删除按钮 40px 可触达）、全局 `:focus-visible`、`:root{color-scheme}`、`prefers-reduced-motion` 兜底、三档响应式断点（1080/720，侧栏收成图标栏）。Fraunces 经 Google Fonts 加载、Georgia/Songti 兜底。
- 注意：此为独立预览原型，尚未改动 `styles.css`/React 源码；若要落地需把 token 移入 `styles.css` 并在各组件用 `var(--token)`，并替换 4 处重复指标网格。等待用户确认方向后再动手改源码。

## 生图功能 V1 开发（按 v14.0 开发文档，分支 feature/image-generation-v1）
- 分支基于 `refactor/repository-slimming-v2` HEAD `c863a54` 创建（用 Python 二进制模式写 packed-refs，避免 LF/CRLF 坑与后台删 loose ref）。每 Phase 独立提交后立即回填 packed-refs 新哈希并 `git log -1` 复核。
- 关键调研结论（前提，务必记住）：① 文档 §12.1 说复用 `runtime-core`，但实测 `packages/runtime-core` 只有 checkpoint-store/runtime-contracts（供 labs），**desktop 实际用的 atomic-write/event-log/run-write-coordinator 在 `apps/desktop/src/main/runtime/*.ts`**。生图 runtime 包（纯 Node，禁依赖 Electron/apps）需要这三件套 → Phase 2 计划把它们移植进 runtime-core 供 import，避免"第二套实现"。② 根 package.json **无 workspaces**，desktop 通过相对路径 import packages（如 `../../../../packages/xxx/src/index`）。③ Reference Anchor 产物在 userData `Masterpiece OS Data/reference-runs/<runId>/outputs/`，不是仓库 projects/。
- **Phase 0（提交 b61adc6 chore: establish v1 boundaries）**：新建 `tests/image-generation/architecture-boundaries.test.js`（5 条前置负向边界断言，目录不存在时空数组不报错）；package.json `test` 脚本加入 `tests/image-generation/*.test.js` 并加 `test:image-generation`。注意根测试是 ESM（package.json `type:module`），测试文件必须用 `import`，不能 `require`（否则 ERR require not defined）。
- **Phase 1（提交 e774d96 feat: add contracts and schemas）**：
  - 新建 `packages/image-generation-contracts`（TS 包，仿 project-contracts，`main/types` 指 src/index.ts）。导出全部 §6/§9/§10/§13/§14 类型：ImageGenerationTask/Run/RunStatus/RunSummary、ImageProviderCapabilities、ImageReferenceRole（4 值对应 §8.4 排序）、三层 Gate 错误码（GateA 8 + GateB 9 + GateC 8 = 25）、Warning 码（8）、ProviderTaskStatus（文档未定义，按异步轮询自设 pending/running/succeeded/failed/cancelled）、GeneratedImage、Review、Retry、Metrics、SourceContextSnapshot、EXECUTING_IMAGE_RUN_STATUSES 常量。
  - `schemas/image-generation/` 下 5 个 draft-2020-12 Schema（capabilities/task/review/gate-result/run，均 additionalProperties:false）。
  - `apps/desktop/src/shared/types.ts` 追加：re-export（`export type{}from`，不进本地作用域）+ import（本地用）两块 + StartImageGenerationInput/RetryImageGenerationInput/ImageGenerationProgress/ImageGenerationCompileResult + `DesktopApi.imageGeneration` 子对象（10 方法 + onRunUpdated）。
  - `apps/desktop/src/preload/index.ts` 加 imageGeneration 转发（forward 到 `image-generation:*` IPC，main handler 待 Phase 5；转发不依赖 handler 存在即可通过 typecheck）。
  - `tests/image-generation/contracts-schema.test.js`（7 条，锁定 Schema 枚举与文档一致）。
  - 自检：desktop typecheck 通过、`test:image-generation` 12 项全过、`verify:current-flows` 9 项全过。
- 待办：Phase 2（runtime：上下文快照/Reference Selector/确定性 Prompt Compiler/三层 Gate/dry-run）→ Phase 3（dashscope provider + 下载校验）→ Phase 4（Headless CLI generate:image + 恢复/重试）→ Phase 5（desktop main image-generation + renderer UI + IPC 注册）→ Phase 6（测试+回归自检）→ 打包 Portable。
