# S4.2-I — Remaining Registry and Runtime Composition

Domain: Settings adapter operations, report runtime, host-neutral service composition and thin Desktop host.

Handlers: 7 settings adapter operations and 2 report business operations. Eleven native filesystem/dialog/shell handlers remain Desktop-only.

Old Owner: Desktop handler closures and Desktop-owned service graph construction.

New Runtime Service: `createRuntimeServices`, `createSettingsOperations`, `createReportOperations` and `report-service.ts`.

Moved: Current service graph construction and report read/rename semantics.

Re-exported: Existing compatibility paths remain unchanged.

Adapters: Electron settings/credentials, safe storage, dialogs, shell, lifecycle, BrowserWindow, IPC and Web RPC event bridge remain Desktop-owned.

Electron imports before: Desktop composition and native adapters only.

Electron imports after: 0 in Shared Runtime and Shared Registry; Desktop imports Shared Runtime bootstrap directly.

Golden coverage: G-01 through G-05.

Tests: Runtime 14/14 PASS; Unit 733/733 PASS; CLI 40/40 PASS; Desktop build PASS; Web Smoke PASS (Electron host); Golden 5/5 PASS; provider calls 0; Golden auto-update NO.

Rollback: Restore Desktop composition and direct settings/report handlers; remove remaining operation factories.

Result: PASS.
