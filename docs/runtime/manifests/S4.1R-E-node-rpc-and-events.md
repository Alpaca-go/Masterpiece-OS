# S4.1R-E — Node RPC, Events, and Lifecycle

Purpose: bind the preserved HTTP RPC/SSE contract to the Shared Registry.  
Old Host: Electron-owned local HTTP server.  
New Host: Node-owned `local-rpc-server.ts`.

Files added: RPC server and 11 Node native operations.  
Electron dependencies before/after: 1 / 0.  
Desktop dependencies before/after: 1 / 0.  
Runtime behavior: business handlers unchanged.  
RPC behavior: health, encoded channel, `{args}`, `{result}`, `{error}`, origin guard, 10 MiB request limit, and SSE event names preserved.  
Tests: Node Host contract plus Unit 736/736; CLI 40/40; Runtime 14/14; Web Smoke PASS.  
Golden: 5/5, no update.  
WEB_SMOKE_HOST: `NODE`.  
Rollback: revert Node RPC binding.  
Result: `PASS`.
