# G-04-01 Visual Analysis

This case preserves the existing JZMX analysis audit, its cryptographically bound fixture manifest, report structure, locked identity semantics, and current model-registry route. It does not call Qwen and never exact-matches analysis prose.
