# S4.2-G — Image Generation, Space and Packaging Runtime

Domain: Standard image generation, Short-Chain space generation, continuation and packaging orchestration.

Handlers: 24 business operations routed through Shared Registry; native `image-generation:open-folder` remains Desktop-only.

Old Owner: Desktop `image-generation/*` business modules and the Image IPC registrar.

New Runtime Service: `@masterpiece/runtime-core/application/image-generation/*` and `createImageGenerationOperations`.

Moved: Context loading, compilation/provider orchestration, run persistence, evidence validation, continuation, post-composition and packaging/space orchestration.

Re-exported: Old Desktop image-generation business paths are `COMPATIBILITY_ONLY`; `ipc.ts` remains a Desktop adapter.

Adapters: Settings, credentials, event sink and native open-folder behavior remain injected by Desktop.

Electron imports before: Type-only Electron import in the Desktop IPC adapter; 0 in business services.

Electron imports after: 0 in Shared Runtime application and operation modules.

Golden coverage: G-01, G-02, G-03 and G-05.

Tests: Runtime 9/9 PASS; Unit 728/728 PASS; CLI 40/40 PASS; Desktop build PASS; Web Smoke PASS (Electron host); Golden 5/5 PASS including G-05; provider calls 0; Golden auto-update NO.

Rollback: Restore the Desktop image-generation directory and IPC registration; remove Shared Image operations.

Result: PASS.
