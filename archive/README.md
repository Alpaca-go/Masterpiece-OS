# Masterpiece Historical Archive

This directory contains historical code, evidence, smoke scripts,
snapshots and superseded implementation artifacts that are no longer
part of the current production runtime.

Archived does not mean deleted.

Do not restore archived code into production paths without reviewing
the corresponding archive manifest and Golden Regression results.

Current runtime source of truth:

- `CURRENT_BASELINE.md`
- `BASELINE_LOCK.md`
