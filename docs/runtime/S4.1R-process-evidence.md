# S4.1R Process Evidence

Status: `PASS`
Captured: 2026-08-11
Platform: Windows / Node.js 24.18.0

## Primary Web Smoke

Command: `npm run web:smoke`

The smoke waited for the host readiness event and HTTP health, opened the real React UI through headless Chrome/CDP, waited until the first renderer child left `splash`, and captured `latest-renderer.png`.

```text
npm smoke runner
|- node apps/web-runtime/src/main.ts
|- node vite --config apps/web/vite.config.mjs
`- chrome headless (browser verification only)
```

Observed with the browser active:

| Metric | Result |
|---|---:|
| Descendant processes inspected | 18 |
| Electron processes | 0 |
| Desktop Main processes | 0 |
| Node Host operations | 147 |
| Renderer state | `page settings-page` |
| Provider calls | 0 |
| Business writes | 0 |

## Actual Web Start

Command: `npm run web:dev` with RPC port 14317 and renderer port 15173.

```text
node run-web-dev.mjs (PID 40356)
|- node tsx apps/web-runtime/src/main.ts
|  `- node apps/web-runtime/src/main.ts
|- node vite --config apps/web/vite.config.mjs
|  `- esbuild service
```

Observed:

| Metric | Result |
|---|---:|
| Process tree entries | 5 |
| Electron processes | 0 |
| Desktop Main processes | 0 |
| Node Host detected | YES |
| Independent Web Renderer detected | YES |
| Health | `{ ok: true, mode: "web", host: "node" }` |
| Renderer HTTP | 200 |
| Project access | PASS |
| Generation options route | PASS |

After termination, the five processes were gone and both ports were closed. No orphan Node process remained.
