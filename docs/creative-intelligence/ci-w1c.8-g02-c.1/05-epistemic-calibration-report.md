# Epistemic calibration report

The deterministic classifier now recognizes generic future-plan language without project or industry special cases.

- Future build/establish/expand language, development plans, roadmaps, visions, and three-to-five-year horizons route to `USER_REQUIREMENT`.
- Forecast language such as expected/projected/likely-to-reach routes to `MODEL_INFERENCE`.
- Current plain declarative statements remain `FACT`.
- Existing precedence remains `UNKNOWN > USER_REQUIREMENT > MODEL_INFERENCE > FACT`.

EPI-G02-01..04 and the earlier EPI-01..06 all pass. No Planning prompt, schema, router, Provider runtime, retry, or timeout policy changed.
