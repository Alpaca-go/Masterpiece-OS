# 2026-07-20

- 拉取远程分支 `feature/visual-translation-v1` 到本地，基于 `9dab2fa`（重写分析协议Phase 4A）。当前工作树干净，分支已切换。
- 精简分析协议 Report Compiler：
  - `compileVisualDirectionsReport` 新增 `mode` 参数（`decision` / `full`），默认 `decision`。
  - Decision Mode 精简报告结构：决策摘要、品牌边界与资产（精简表）、核心视觉依据（Signals+Opportunities 合并最多5条）、行业模板简表、Direction 精简至最多12字段、Difference Matrix 简表、评分推荐合并、Evidence Index 精简、Metadata 精简。
  - Full Mode 保留原有完整输出，用于调试/审计/QA。
  - 修复内容冲突：restricted certification_badge 不得描述为可生成徽章；缺失母品牌VI时不得输出色彩关联建议；保留 `brand_specific_mechanism`。
  - `run-visual-translation-v1.js` 默认使用 `decision` 模式，支持 `input.reportMode` 覆盖。
  - 测试已更新显式传入 `reportMode: 'full'`，全部72个测试通过。
  - 未提交 git。
- 重新打包桌面 portable 客户端：
  - 清理旧构建产物后执行 `npm run build` 和 `node scripts/package-win.mjs`
  - 生成 `Masterpiece-OS-Desktop-Portable-0.1.0-x64.exe`（107MB），包含最新精简 Report Compiler
  - 未运行 `verify:document-flows`（未提交 git，跳过 prepackage 钩子）
- 修复 `02-visual-signal-opportunity` 运行时失败（B2B 项目 aesthetic-tension 缺失）：
  - `visual-strategy-signal-map-v1.js`：emotion/culture/aesthetic-tension 改为可选 0-3 条（合计至少 1 条）；总信号数 min: 7 → min: 5
  - `visual-signal-opportunity-prompt-v1.js`：同步更新 prompt 约束说明
  - 强制类型（audience-boundary、capability、relationship）仍保持 1-3 条要求
- 全面审查分析协议潜在报错点：
  - `visual-opportunity-map-v1.js`：categoryCliches min: 2 → min: 1（独特项目可能只有 1 个模板风险）
  - `visual-evidence-map-v1.js`：排除 `suggestedAssets.name` 的中文语言检查（资产名称可为英文术语）
- 重新打包桌面 portable 客户端（第 4 次，12:29）：包含 v1.3.1 热修复 + qwen-reasoner 参数兼容修复，文件大小 107MB。
- 修复客户端运行时 `Decision Report 渲染验证失败`（12:36）：
  - `markdown_adhesion`：标题粘连（缺少前导空行）— 在 `sanitizeDecisionReport` 中增加通用修复规则 `text.replace(/([^\n])(#{2,3}\s)/g, '$1\n\n$2')`，处理所有非句号结尾的标题粘连
  - `enum_leak`：`evidence_basis` / `business_model` / `consumer_visual_policy` 等 snake_case 值泄漏到正文
    - 在 `DISPLAY_LABELS` 中增加大量映射：`evidence_basis`/`derived_evidence`/`confirmed_evidence`/`reasonable_inference`/`suggested_evidence`/`business_model`/`consumer_visual_policy`/`b2b`/`b2c`/`auxiliary_only` 等
    - 在 `compileFullReport` 中对 `strategic_factors` 和 `penalty_reasons` 使用 `mapDisplayLabelsInText` 映射为中文
    - 在 `sanitizeDecisionReport` 的 `enum_leak` 替换中排除 `evidence_basis`
    - 在 `validateDecisionReportRender` 的 `enum_leak` 检查中排除 `evidence_basis` 及相关证据类型
  - 全部 72 个测试通过
  - 未提交 git
- **Visual Directions Decision Report v1.3.1 热修复（11:00）：**
  - `compile-visual-directions-report.js`：全面升级 Decision Report Compiler
    - P0：新增 `getDirectionDisplayName` 统一名称解析（防 undefined）、`formatScore` 统一 1 位小数格式、`sanitizeDecisionReport`/`validateDecisionReportRender` 最终输出清理与验证
    - P0：报告版本升级至 `v1.3.1`，`report_mode: decision`；`primary_language_ratio` 去重，显示百分比
    - P0：Sprint 2 后置说明只出现一次
    - P1：核心视觉依据分类按信号类型映射（受众边界、能力结构、生态连接、品牌情绪、品牌文化、审美张力），并补充 evidence 的合规与信任/资产边界
    - P1：Direction 触点精简至最多 3 个（2 个 B2B core + 1 个辅助）
    - P1：B2B + auxiliary_only 时人物政策默认禁止女性面部特写
    - P1：关键风险最多 2 条，统一 Markdown 嵌套列表格式
    - P1：摘要推荐理由至少包含两类决策因素（品牌匹配、证据强度、模板风险、延展性、对比优势）
    - P1：Recommendation 正文包含方向间比较，从 view.directions 查找 direction 名称
    - 方向名称统一格式：`D03「衡石·筑阶」`
  - `run-visual-translation-v1.js`：在 `compileVisualDirectionsReport` 后调用 `sanitizeDecisionReport` + `validateDecisionReportRender`
  - 修复 `sanitizeDecisionReport` 中 `\s{2,}` 破坏 Markdown 换行的问题（改为 `[ \t]{2,}`）
  - 修复 `compileDecisionReport` 中 `primary_language_ratio` 使用空字符串测量导致错误值的问题
  - 修复 `buildDecisionRecommendationText` 中 comparison item 没有 `name` 字段导致 `未命名方向` 的问题
  - 全部 72 个测试通过，`verify:document-flows` 通过，`git diff --check` 通过
  - **Visual Directions Decision Report v1.3.3 稳定性热修复（09:35）：**
  - `compile-visual-directions-report.js`：基于 v1.3.3 开发文档完成全部修改
    - 重构 Markdown Renderer 使用结构化 Block API（`renderHeading`, `renderBlockquote`, `renderBulletList`, `renderNestedRiskList`, `renderMarkdownTable`, `joinMarkdownBlocks`）
    - 消除孤立 `#` 标题、修复 Markdown 粘连（标题/blockquote/list）、移除 bullet 间多余空行、修复嵌套风险列表层级
    - 重构 Recommendation Compiler：禁止截断自然语言字段（`brand_specific_mechanism`, `core_metaphor`, `direction_summary`, `visual_language`, `risk_text`）
    - 新增 Recommendation Comparison Contract 与结构化优势类型（`RECOMMENDATION_ADVANTAGE_LABELS`）
    - 实现推荐强度解析器（`getRecommendationStrength`，按 `score_gap` 分 strong/normal/weak/tied）
    - 差异矩阵 Key/Value 映射为中文（`DIFFERENCE_MATRIX_KEY_LABELS`）
    - 中文本地化所有品牌边界字段（`DISPLAY_LABELS`）
    - 低分方向显示逻辑（<50=不建议进入Sprint 2，<60=低优先级）
    - 增强 Sanitizer/Validator：括号平衡检查、孤立标题检测、bullet 空行检查、内部字段泄漏检查
    - 修复 `cleanText` 中 `/{2,}/g` 正则语法错误（应为 `/{2,}/g`）
    - 修复 `renderBlockquote` 多行文本时生成孤立标题的问题（逐行加 `> ` 前缀）
    - 修复 `markdown_adhesion` 验证器误报（`[^ \n]#{2,3}\s` → `[^ \n#]#{2,3}\s`）
    - 修复 `bullet_extra_blank` 报错：根因是 `sanitizeDecisionReport` 中 `clean punctuation spacing` 使用 `\s+` 破坏换行符（`text.replace(/。\s+/g, '。')` 把 `。\n` 吃掉），与 `fix dot-list adhesion` 交互产生多余空行。修复：改为 `text.replace(/。[ \t]+/g, '。')` 仅清理水平空白
    - 修复 `duplicate_metadata_key` 误报：sanitizer 和 validator 的 `^ - (\w+).*$` 正则过宽，误删/误报正文 bullet（如 `Sprint`, `Do`）。修复：引入 `METADATA_KEYS` 白名单（仅 10 个元数据 key），sanitizer 和 validator 同步限制
    - 移除 validator 中的 debug `console.error` 语句
    - 修复 `compileDecisionReport` blockquote 模板尾部多余空格（git diff --check 告警）
  - 全部 265 个测试通过，`verify:document-flows` 通过，`git diff --check` 通过
  - 0 次真实 Provider 调用，未提交 git
- **重新打包桌面 portable 客户端（13:38）：** 用户主动要求打包，执行 `npm run desktop:package`，`verify:document-flows` 通过，TypeScript + electron-vite build 成功，electron-builder 生成 `release/Masterpiece-OS-Desktop-Portable-0.1.0-x64.exe`（107MB），包含 v1.3.3 全部稳定性热修复。文件路径：`E:\Masterpiece-OS\apps\desktop\release\Masterpiece-OS-Desktop-Portable-0.1.0-x64.exe`
- **客户端首页为视觉转译记录添加删除按钮（14:01）：**
  - 后端：`visual-translation-service.ts` 新增 `remove(runId)` 方法，删除本地运行文件夹（含 input、checkpoints、outputs、runtime），若任务正在运行则先 abort 再删除；引入 `assertInside` 安全边界校验
  - 类型：`shared/types.ts` 在 `DesktopApi.visualTranslation` 中新增 `remove(runId: string): Promise<void>`
  - 桥接：`preload/index.ts` 暴露 `visual-translation:remove` IPC 通道
  - 主进程：`main/index.ts` 注册 `visual-translation:remove` handler，检查 running 状态后调用服务删除
  - 前端：`App.tsx` 新增 `deletingRunId` 状态与 `deleteTranslationRun` 函数，UI 中将 `visual-translation` 记录的占位 `<span className="record-spacer" />` 替换为与 `visual-analysis` 一致的删除按钮（含确认弹窗、禁用 running 状态、删除中加载态）
- 修复 `04-three-creative-directions` 阶段 `RESTRICTED_ASSET_EXECUTION` 报错（中医馆项目）：
  - 根因：模型返回的 `executableAssetIds` 包含不在白名单中的 asset ID（证据地图中 `executable=true` 且 `execution_scope=current_direction` 的 suggestedAssets 才允许）
  - `visual-creative-directions-v1.js`：改进错误信息，列出非法 asset ID 和允许的 asset ID 白名单；添加 `repairDirectionIds` 供修复循环精确定位
  - `run-visual-translation-v1.js`：修复提示词明确告诉模型移除非法 ID、仅使用证据上下文中的 executable 资产、无可用时留空数组
  - `visual-directions-prompt-v1.js`：在提示词中明确列出当前可用的 executable asset ID 白名单（`SA001, SA002...` 或 `none`）
  - 全部 265 个测试通过
- **重新打包桌面 portable 客户端（14:34）：** 包含 v1.3.3 稳定性修复 + 视觉转译记录删除按钮 + `RESTRICTED_ASSET_EXECUTION` 修复。文件路径：`E:\Masterpiece-OS\apps\desktop\release\Masterpiece-OS-Desktop-Portable-0.1.0-x64.exe`
